# Implement Task — __PROJECT_NAME__

Read these files first:

- `AGENTS.md`
- `TASKS.md`
- `DB_API_CONTRACT.md`
- `ERROR_CODES.md`
- `TESTING_STRATEGY.md`
- `SCENARIOS.md`

## Rules

- Implement only the requested task from `TASKS.md`.
- Business logic belongs in PostgreSQL.
- API routes are thin.
- React renders contract data.
- Update contract docs when behavior changes.
- Run the required tests for the task type before finishing.

## Allowed files

Use only the files listed in the task definition and files directly required by that task.

## Forbidden changes

- Do not implement unrelated tasks.
- Do not move business logic into Express or React.
- Do not invent undocumented API fields or error codes.
- Do not skip required tests.

## Your task

Implement the requested task exactly as defined in `TASKS.md`.

## Completion summary

Stop after the task is complete and include:

1. files changed
2. tests added
3. commands run
4. assumptions
5. recommended next task
